const str = 'abc';
const number = 1;
const boolean = true;
const obj = {
	outside: {
		inside: {
			key: ' value',

		}
	}
}

console.log(str);
console.log(number);
console.dir(obj);

console.error("에러발생");
console.trace("에러 위치 추적");